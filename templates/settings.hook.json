{
  "_comment": "Reference only. setup.sh merges these into .claude/settings.json for you (without clobbering existing hooks). PreToolUse:Bash is installed ONLY when the repo has notebooks + the shadow module. Stop is opt-in (setup.sh asks). Delete this _comment key if you hand-merge.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && python3 tools/notebook_graphify_shadow.py hook 2>/dev/null || true"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && graphify update . >/dev/null 2>&1 || true"
          }
        ]
      }
    ]
  }
}
